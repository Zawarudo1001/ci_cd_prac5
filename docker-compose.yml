version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:10000"
    environment:
      - PORT=10000
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped